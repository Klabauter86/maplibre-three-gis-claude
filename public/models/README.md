# GLB/GLTF models

Place future tree and rock assets here, for example:

- `tree-spruce.glb`
- `tree-pine.glb`
- `tree-deciduous.glb`
- `rock-a.glb`

The current starter deliberately uses procedural low-poly geometry so the repository runs without binary assets.
